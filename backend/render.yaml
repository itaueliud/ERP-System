# Render configuration for backend deployment
# Docs: https://render.com/docs/deploy-node-express-app

buildCommand: npm install && npm run build
startCommand: npm run start
environment:
  - key: NODE_ENV
    value: production
  # Add your database URL and other secrets here
  # - key: DATABASE_URL
  #   value: <your-render-db-url>
