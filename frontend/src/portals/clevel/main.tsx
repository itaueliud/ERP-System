import React from 'react';
import ReactDOM from 'react-dom/client';
import CLevelApp from './CLevelApp';
import '../../index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CLevelApp />
  </React.StrictMode>
);
